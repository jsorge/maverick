version: "3.3"
services:
  maverick:
    build:
      context: .
      dockerfile: Dockerfile-dev
    image: maverick:dev
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    entrypoint: make docker-run
  nginx:
    build:
      context: .
      dockerfile: ./_dev/nginx/Dockerfile-nginx
      args:
        build_domain: jsorge.local
    environment:
      CA_SSL: "false" # change to "true" for production
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./_dev/Public:/home/maverick/Public
      - ./_dev/nginx/ssl:/var/www/ssl
      - ./_dev/nginx/logs:/var/log/nginx
    depends_on:
      - web
  
